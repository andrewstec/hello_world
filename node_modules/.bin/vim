#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vim/bin/vim" "$@"
  ret=$?
else 
  node  "$basedir/../vim/bin/vim" "$@"
  ret=$?
fi
exit $ret
